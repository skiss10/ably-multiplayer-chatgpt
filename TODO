1) Add an architecture of Ably channels

2) Fix UI for Chat GPT to look the setMessages

3) Fix Square to be same size as the chat box (and add two random letters in the box like a simulated first and last name)


